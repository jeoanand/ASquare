# Google reCAPTCHA v2 site key for the contact form (contact.astro).
# Register a key for your domain at https://www.google.com/recaptcha/admin
# and set it below (or in .env) before deploying. Without it, the contact
# form falls back to Google's shared test key, which always passes.
PUBLIC_RECAPTCHA_SITE_KEY=
